- [x] FEAT-20260921-2129 — Establish the generated Django and Next.js foundations, pin Python 3.12.2, validate required configuration, add the custom email user model before initial migrations, and configure project-local quality tooling (spec: docs/specs/FEAT-20260921-2129-project-foundation.md) [2026-09-21 22:42]
- [x] FEAT-20260921-2130 — Implement secure email/password registration and JWT session lifecycle with memory-only access tokens, rotating HTTP-only refresh cookies, CSRF/origin protection, logout blacklisting, and current-user retrieval (spec: docs/specs/FEAT-20260921-2130-authentication.md) [2026-09-21 22:59]
- [x] FEAT-20260921-2131 — Implement user-owned expense persistence plus validated creation and paginated category/date-filtered listing APIs using integer minor units throughout business logic (spec: docs/specs/FEAT-20260921-2131-expense-api.md) [2026-09-21 23:10]
- [x] FEAT-20260921-2132 — Implement authenticated monthly summaries with total and per-category spend, previous-month comparison, explicit zero-baseline behavior, and greater-than-20-percent category insights (spec: docs/specs/FEAT-20260921-2132-summary-api.md) [2026-09-21 23:18]
- [x] FEAT-20260921-2133 — Implement routed Next.js registration/login and protected application shell with memory-only access-token state, cookie-backed session restoration, coordinated refresh, and safe redirects (spec: docs/specs/FEAT-20260921-2133-frontend-auth.md) [2026-09-21 23:40]
- [x] FEAT-20260921-2134 — Implement routed expense creation, paginated URL-filtered expense listing, and URL-selected monthly summary views with centralized money formatting and complete loading/empty/error states (spec: docs/specs/FEAT-20260921-2134-frontend-expenses.md) [2026-09-21 23:57]
- [x] ISS-20260921-2134 — Complete cross-stack verification and submission documentation, including recursive scope checks, production builds, migration checks, root README instructions and decisions, future-work notes, and the required AI-use disclosure (spec: docs/specs/ISS-20260921-2134-verification-documentation.md) [2026-09-22 00:16]
- [x] ISS-20260921-2243 — Normalize repository text files to LF and enforce the line-ending policy so generated CRLF files cannot make whitespace validation unreliable (found while closing FEAT-20260921-2129; spec: docs/specs/ISS-20260921-2243-line-endings.md) [2026-09-21 22:47]
- [x] FEAT-20260922-0842 — Add a Windows/Linux-portable production-style Docker stack with non-root Django and Next.js images, a Docker-managed SQLite volume, ordered migrations and health checks, explicit configuration, and documented lifecycle commands (ADR: docs/adr/0002-portable-container-runtime.md; spec: docs/specs/FEAT-20260922-0842-container-stack.md) [2026-09-22 09:16]
- [x] FEAT-20260922-0843 — Add a Bash verification entry point that mirrors scripts/check.ps1, supports Unix and Git Bash virtual-environment layouts explicitly, and document the two supported check commands (spec: docs/specs/FEAT-20260922-0843-bash-check-script.md) [2026-09-22 09:18]
- [x] ISS-20260922-1215 — Fix a frontend Docker build failure caused by `.dockerignore` excluding `vitest.setup.ts`/`vitest.config.mts`, which dropped the jest-dom type augmentation and broke `next build`'s type check; also log the local `compose.yaml` → `docker-compose.yaml` rename and 8020/3020 port remap made while first executing the container stack (spec: docs/specs/ISS-20260922-1215-container-stack-first-run.md) [2026-09-22 12:15]
- [x] ISS-20260922-1345 — Fix `getApiBaseUrl` reading `NEXT_PUBLIC_API_URL` through an indirect `environment.NEXT_PUBLIC_API_URL` access that Next.js's client-bundle inliner never matches, which threw "NEXT_PUBLIC_API_URL is required" in the browser regardless of the configured value; verified against the full Compose stack (spec: docs/specs/ISS-20260922-1345-client-env-inlining.md) [2026-09-22 13:45]
- [x] ISS-20260922-1420 — Fix `ApiClient` capturing a detached global `fetch`, which threw "Illegal invocation" on every API call in a real browser (found via a Canary browser-automation session, invisible to jsdom-based tests and curl smoke tests); added a receiver-branding regression test and re-verified the full login/summary/expenses/logout flow with a second Canary session against the rebuilt stack (spec: docs/specs/ISS-20260922-1420-client-fetch-illegal-invocation.md) [2026-09-22 14:20]
- [x] FEAT-20260922-1530 — Add an idempotent `seed_demo_data` management command that creates a fixed demo account with sample expenses (exercising both bonus insight types) on every server start, so the app stays demoable on hosts like Render that discard the database on redeploy, without blocking normal registration of separate accounts (spec: docs/specs/FEAT-20260922-1530-demo-seed-command.md) [2026-09-22 15:30]
- [x] ISS-20260922-1600 — Set `SECURE_PROXY_SSL_HEADER` so `DJANGO_SECURE_SSL_REDIRECT=true` behind Render's TLS-terminating reverse proxy doesn't loop-redirect every request forever; found and fixed before attempting the actual deployment (spec: docs/specs/ISS-20260922-1600-proxy-ssl-header.md) [2026-09-22 16:00]
- [x] FEAT-20260922-1700 — Make the backend image self-migrating via a new `entrypoint.sh` (migrate, seed, then Gunicorn bound to `$PORT`), since Render's free tier has no separate one-shot service, Pre-Deploy Command, or console access; simplified `docker-compose.yaml` by removing the now-redundant (and, once `ENTRYPOINT` replaced `CMD`, actively broken) separate `migrate` service (spec: docs/specs/FEAT-20260922-1700-self-migrating-backend-entrypoint.md) [2026-09-22 17:00]
