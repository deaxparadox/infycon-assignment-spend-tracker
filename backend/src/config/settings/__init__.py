"""Django settings package.

Select an explicit module such as ``config.settings.development`` or
``config.settings.test``. Production entry points use ``base`` and therefore
require all security-sensitive environment variables.
"""
